# Starknet Escrow Environment Variables

# Contracts Deployment
# OWNER_ADDRESS: The address that will OWN the escrow contract and can withdraw accumulated fees
# This is a constructor parameter that gets stored in the contract
# Can be any Starknet address (doesn't need to be the deployer)
OWNER_ADDRESS=0x0...

FEE_BPS=100
NETWORK=sepolia

# Braavos Account Configuration (required for deployment)
# ACCOUNT_ADDRESS: Your Braavos wallet address that will DEPLOY the contract
# PRIVATE_KEY: The private key of your Braavos account (used to sign deployment transactions)
# Note: ACCOUNT_ADDRESS is who pays for deployment, OWNER_ADDRESS is who controls the contract
PRIVATE_KEY=0x...
ACCOUNT_ADDRESS=0x...

# Deployed Contract Address (used by verify script)
ESCROW_CONTRACT_ADDRESS=0x0...

# Server (if any)
# DATABASE_URL=postgresql://user:password@localhost:5432/starkescrow
